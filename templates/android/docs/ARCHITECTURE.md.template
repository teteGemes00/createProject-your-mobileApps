# Architecture - {{ project_name }}

## Project Structure

```
{{ project_name }}/
├── app/
│   └── src/
│       └── main/
│           ├── java/{{ package_name }}/
│           │   └── MainActivity.{{ language_ext }}
│           ├── res/
│           │   ├── layout/
│           │   ├── values/
│           │   └── mipmap-*/
│           └── AndroidManifest.xml
├── gradle/
│   └── wrapper/
├── build.gradle
└── settings.gradle
```

## Core Components

### MainActivity
The single entry-point Activity for the application.

## Build Configuration

- **Min SDK**: {{ min_sdk }}
- **Target SDK**: {{ target_sdk }}
- **Compile SDK**: {{ compile_sdk }}
- **JDK**: {{ jdk_version }}
- **Language**: {{ language }}

## Dependencies

See `app/build.gradle` for the complete dependency list.

## Testing

- Unit tests: `app/src/test/`
- Instrumented tests: `app/src/androidTest/`
